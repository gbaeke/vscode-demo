---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: super-api
  namespace: flux-system
spec:
  chart:
    spec:
      chart: ./chart/go-template
      sourceRef:
        kind: GitRepository
        name: super-api
  interval: 1m0s
  targetNamespace: default

